version: "3.8"

services:
  postgres:
    image: postgres:13
    restart: always  # Ensures Postgres restarts on failure
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: trustid
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  app:
    build: .
    restart: always  # Ensures app restarts if it crashes
    depends_on:
      - postgres
    env_file:
      - .env  # ✅ Load environment variables from .env file
    ports:
      - "8000:8000"
    volumes:
      - ./keys:/app/keys  # Persist keys across restarts
      - ./entrypoint.sh:/app/entrypoint.sh  # Ensures entrypoint script is mounted properly
    entrypoint: ["/bin/sh", "/app/entrypoint.sh"]

volumes:
  postgres_data:
